name: CI

on:
  pull_request:

jobs:
  version-check:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Check TypeScript SDK version bump
        run: ./scripts/check-pr-version-bump.sh
